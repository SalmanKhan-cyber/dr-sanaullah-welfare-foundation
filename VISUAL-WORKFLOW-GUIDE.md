# 🎨 Visual Workflow Guide

Complete visual guide showing how all parts of the system work together.

---

## 🌐 System Architecture Flow

```
┌─────────────────────────────────────────────────────────────┐
│                         USER (Browser)                      │
│                   http://localhost:5173                     │
└────────────────┬────────────────────────────────────────────┘
                 │
                 │ Views Pages
                 ▼
┌─────────────────────────────────────────────────────────────┐
│                    REACT FRONTEND (Vite)                    │
│  ┌────────┬────────┬────────┬────────┬────────┬────────┐  │
│  │ Home   │ Login  │ Donate │ About  │Contact │Dashbrd │  │
│  └────────┴────────┴────────┴────────┴────────┴────────┘  │
│                                                              │
│  Components: Navbar, Forms, FileUpload, Dashboards         │
└────────────────┬────────────────────────────────────────────┘
                 │
                 │ API Calls (fetch)
                 │ Authorization: Bearer TOKEN
                 ▼
┌─────────────────────────────────────────────────────────────┐
│              EXPRESS.JS BACKEND (Node.js)                   │
│                 http://localhost:4000                       │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Middleware Chain                                     │  │
│  │  1. CORS Check        ✓                              │  │
│  │  2. Auth Middleware   → Validate JWT                 │  │
│  │  3. RBAC Middleware   → Check user role              │  │
│  │  4. Route Handler     → Process request              │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  Routes:                                                    │
│  • /api/auth/*         - Authentication                    │
│  • /api/donations/*    - Donation management              │
│  • /api/courses/*      - Course management                │
│  • /api/lab/*          - Lab reports                      │
│  • /api/pharmacy/*     - Pharmacy inventory               │
│  • /api/patients/*     - Patient data                     │
│  • /api/notifications/*- Notifications                    │
└────────────────┬────────────────────────────────────────────┘
                 │
                 │ Supabase Client
                 ▼
┌─────────────────────────────────────────────────────────────┐
│                   SUPABASE (Cloud)                          │
│         https://qudebdejubackprbarvc.supabase.co           │
│                                                              │
│  ┌──────────────┬──────────────┬──────────────────────┐   │
│  │              │              │                       │   │
│  │ PostgreSQL   │  Auth        │  Storage              │   │
│  │ Database     │  (JWT)       │  (Files)              │   │
│  │              │              │                       │   │
│  │ • users      │ • Sign up    │ • lab-reports         │   │
│  │ • patients   │ • Login      │ • prescriptions       │   │
│  │ • donations  │ • OTP        │ • certificates        │   │
│  │ • courses    │ • Tokens     │ • receipts            │   │
│  │ • pharmacy   │              │                       │   │
│  │ • 12 tables  │              │ (Signed URLs)         │   │
│  │              │              │                       │   │
│  │ + RLS        │              │                       │   │
│  └──────────────┴──────────────┴──────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
Step 1: User Signs Up
┌──────────┐
│  User    │  Enters email + password
│  (Browser)│  or phone number
└────┬─────┘
     │
     │ POST /api/auth/signup-email
     │ or POST /api/auth/otp
     ▼
┌──────────────┐
│   Backend    │  Forwards to Supabase Auth
└────┬─────────┘
     │
     ▼
┌──────────────┐
│  Supabase    │  Creates user account
│  Auth        │  Sends verification email/OTP
└────┬─────────┘
     │
     │ Returns user + session
     ▼
┌──────────────┐
│   Backend    │  Stores session
└────┬─────────┘
     │
     │ Returns JWT token
     ▼
┌──────────────┐
│  Frontend    │  Stores token
│              │  Redirects to dashboard
└──────────────┘

Step 2: Admin Sets Role
┌──────────────┐
│    Admin     │  Goes to Supabase dashboard
└────┬─────────┘
     │
     │ Finds user
     │ Edits user_metadata
     │ Sets: { "role": "patient" }
     ▼
┌──────────────┐
│  Supabase    │  Updates user metadata
└────┬─────────┘
     │
     ▼
User refreshes → Role applied ✓
```

---

## 💰 Donation Workflow

```
┌──────────────────────────────────────────────────────────┐
│  Step 1: Donor Visits Donation Page                     │
│  http://localhost:5173/donation                          │
└───────────────┬──────────────────────────────────────────┘
                │
                │ Fills form:
                │ • Purpose: Medical
                │ • Amount: 5000
                ▼
┌──────────────────────────────────────────────────────────┐
│  Step 2: Submit Donation                                 │
│  POST /api/donations                                     │
│  { amount: 5000, purpose: "medical" }                   │
└───────────────┬──────────────────────────────────────────┘
                │
                │ Backend validates
                │ Checks auth token
                │ Checks role = "donor"
                ▼
┌──────────────────────────────────────────────────────────┐
│  Step 3: Process Payment (Stub)                          │
│  // const paymentIntent = await stripe.create()         │
│  (In production: real payment gateway)                  │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────┐
│  Step 4: Save to Database                                │
│  INSERT INTO donations                                   │
│  (donor_id, amount, purpose)                            │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────┐
│  Step 5: Generate Receipt                                │
│  • Create HTML receipt                                   │
│  • (Optional) Convert to PDF                            │
│  • Upload to receipts bucket                            │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────┐
│  Step 6: Send Notification                               │
│  INSERT INTO notifications                               │
│  "Thank you for your donation of PKR 5000!"             │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌──────────────────────────────────────────────────────────┐
│  Step 7: Return Success                                  │
│  Frontend shows: "Donation successful!"                  │
│  Redirects to /dashboard/donor                          │
└──────────────────────────────────────────────────────────┘
```

---

## 🧪 Lab Report Workflow

```
┌─────────────┐
│  Lab Staff  │  Logs in with lab@dswf.org
└──────┬──────┘
       │
       │ Goes to /dashboard/lab
       ▼
┌──────────────────────────────────────────────────┐
│  Lab Dashboard                                   │
│  • View test requests                           │
│  • Upload report button                         │
└──────┬───────────────────────────────────────────┘
       │
       │ Click "Upload Report"
       │ Select patient
       │ Choose PDF file
       │ Add remarks
       ▼
┌──────────────────────────────────────────────────┐
│  POST /api/lab/reports/upload                    │
│  FormData:                                       │
│    - file: report.pdf                           │
│    - patientId: abc123                          │
│    - remarks: "Blood test normal"               │
└──────┬───────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────┐
│  Backend Processing                              │
│  1. Validate file (PDF, < 20MB)                 │
│  2. Generate unique path                        │
│     patients/abc123/1730000000-report.pdf       │
└──────┬───────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────┐
│  Upload to Supabase Storage                      │
│  Bucket: lab-reports                            │
│  Path: patients/abc123/...                      │
└──────┬───────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────┐
│  Save to Database                                │
│  INSERT INTO lab_reports                         │
│  (patient_id, file_url, remarks)                │
└──────┬───────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────┐
│  Generate Signed URL                             │
│  7-day expiry for security                      │
└──────┬───────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────┐
│  Notify Patient                                  │
│  INSERT INTO notifications                       │
│  "Your lab report is ready"                     │
└──────┬───────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────┐
│  Patient Receives Notification                   │
│  • Bell icon shows unread (1)                   │
│  • Click to view notification                   │
│  • "Download Report" button                     │
└──────┬───────────────────────────────────────────┘
       │
       │ Patient clicks download
       ▼
┌──────────────────────────────────────────────────┐
│  GET /api/lab/reports/:id/download               │
│  Returns signed URL                              │
└──────┬───────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────┐
│  Patient Downloads PDF                           │
│  Secured, time-limited access                   │
└──────────────────────────────────────────────────┘
```

---

## 🎓 Student Enrollment Workflow

```
Step 1: Teacher Creates Course
┌──────────────┐
│   Teacher    │  POST /api/courses
└──────┬───────┘  { title, description, duration }
       │
       ▼
┌──────────────────────────────────────────┐
│  Database: courses table                 │
│  • Ultrasound Training                  │
│  • Discount: 70%                        │
│  • Duration: 3 months                   │
└──────┬───────────────────────────────────┘
       │
       ▼
Step 2: Student Browses Courses
┌──────────────┐
│   Student    │  GET /api/courses
└──────┬───────┘  Views all available courses
       │
       ▼
┌──────────────────────────────────────────┐
│  Course List Display                     │
│  ✓ Ultrasound Training - 3 months       │
│    Original: 100,000 PKR                │
│    Discounted: 30,000 PKR (70% off!)    │
│    [Enroll Now]                         │
└──────┬───────────────────────────────────┘
       │
       │ Student clicks Enroll
       ▼
Step 3: Enrollment
┌──────────────────────────────────────────┐
│  POST /api/courses/enroll                │
│  { course_id: "course-uuid" }           │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  Database: students table                │
│  INSERT (user_id, course_id)            │
│  Progress: 0%                           │
└──────┬───────────────────────────────────┘
       │
       ▼
Step 4: Track Progress
┌──────────────────────────────────────────┐
│  Teacher marks progress                  │
│  UPDATE students SET progress = 50      │
└──────┬───────────────────────────────────┘
       │
       ▼
Step 5: Issue Certificate (100% complete)
┌──────────────────────────────────────────┐
│  POST /api/certificates/issue            │
│  • Upload certificate file              │
│  • Set progress = 100                   │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  Supabase Storage: certificates bucket   │
│  Path: students/user-id/cert.pdf        │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  Student Downloads Certificate           │
│  GET /api/certificates/:id/download      │
└──────────────────────────────────────────┘
```

---

## 💊 Pharmacy Prescription Workflow

```
┌─────────────┐
│   Doctor    │  Creates prescription
└──────┬──────┘  (via admin or separate system)
       │
       ▼
┌──────────────────────────────────────────┐
│  Database: prescriptions table           │
│  • Patient: John Doe                    │
│  • Medicine: Paracetamol 500mg          │
│  • File: prescription.pdf               │
└──────┬───────────────────────────────────┘
       │
       │ Notification sent to:
       ├─────────────┬────────────────┐
       ▼             ▼                ▼
   Patient       Pharmacy        Doctor
       │
       ▼
┌──────────────────────────────────────────┐
│  Patient Views Prescription              │
│  /dashboard/patient                      │
│  Downloads prescription file            │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  Pharmacy Processes                      │
│  /dashboard/pharmacy                     │
│  • Views prescription                   │
│  • Checks inventory                     │
│  • Applies 50% discount                 │
│  • Dispenses medicine                   │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  Update Inventory                        │
│  UPDATE pharmacy_items                   │
│  SET stock = stock - quantity           │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  Low Stock Alert (if needed)             │
│  IF stock < 100:                        │
│    Notify admin + pharmacy              │
└──────────────────────────────────────────┘
```

---

## 🔔 Notification System Flow

```
Trigger Events:
┌─────────────────────────────────────────┐
│  • Lab report uploaded                  │
│  • Donation confirmed                   │
│  • Certificate issued                   │
│  • New prescription                     │
│  • Low stock alert                      │
│  • Course enrollment                    │
└──────┬──────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  Backend Creates Notification            │
│  INSERT INTO notifications               │
│  (user_id, message, created_at)         │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  Frontend Fetches Notifications          │
│  GET /api/notifications                  │
│  On page load / periodic polling        │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  Display in UI                           │
│  • Bell icon with badge (3)             │
│  • Notification dropdown                │
│  • Mark as read button                  │
└──────┬───────────────────────────────────┘
       │
       │ User clicks notification
       ▼
┌──────────────────────────────────────────┐
│  Mark as Read                            │
│  POST /api/notifications/read            │
│  UPDATE notifications SET read = true   │
└──────────────────────────────────────────┘
```

---

## 🛡️ Security & RBAC Flow

```
Request Flow with Security:

1. User Sends Request
   ↓
   GET /api/donations/all
   Headers: { Authorization: "Bearer eyJhbGc..." }

2. CORS Middleware
   ↓
   ✓ Check origin = http://localhost:5173
   ✓ If not allowed → 403 Forbidden
   ✓ If allowed → Continue

3. Auth Middleware (middleware/auth.js)
   ↓
   • Extract token from header
   • Validate with Supabase Auth
   • Get user details
   • Set req.user = { id, email, role }
   • If invalid → 401 Unauthorized
   • If valid → Continue

4. RBAC Middleware (middleware/rbac.js)
   ↓
   • Check req.user.role
   • Compare with allowed roles: ['admin']
   • If role matches → Continue
   • If not → 403 Forbidden

5. Route Handler
   ↓
   • Process request
   • Query database
   • Return response

Example RBAC Configuration:
┌──────────────────────────────────────────┐
│  Route: /api/donations/all               │
│  Allowed: ['admin']                      │
│                                          │
│  ✓ Admin → Access granted               │
│  ✗ Donor → 403 Forbidden                │
│  ✗ Patient → 403 Forbidden              │
└──────────────────────────────────────────┘
```

---

## 📁 File Upload Security

```
Upload Flow:

1. User Selects File
   ↓
   FileUpload component
   Max size: 20MB
   Allowed: PDF, JPEG, PNG

2. Frontend Validation
   ↓
   • Check file size
   • Check MIME type
   • If invalid → Show error
   • If valid → Upload

3. Backend Receives File
   ↓
   Multer middleware
   • Validates size again
   • Stores in memory buffer
   • Passes to route handler

4. Backend Processes
   ↓
   • Verify user permission
   • Generate unique path
   • patients/{id}/{timestamp}-{filename}

5. Upload to Supabase Storage
   ↓
   • Bucket: lab-reports (private)
   • Path: generated path
   • Content-Type: application/pdf

6. Generate Signed URL
   ↓
   • 7-day expiry
   • Temporary access
   • Secured URL

7. Store Path in Database
   ↓
   • Save file_url = path
   • Link to patient record

8. Return Success
   ↓
   • Frontend shows success
   • File available for download
```

---

## 🔄 Data Flow Summary

```
User Action → Frontend → Backend → Database/Storage → Response

Example: View Lab Reports
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Patient │────>│ Frontend │────>│ Backend  │────>│ Supabase │
│  clicks  │     │  calls   │     │ validates│     │  queries │
│ "Reports"│     │   API    │     │   role   │     │ database │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
                                        │                │
                                        │<───────────────┘
                                        │  Returns data
                                        ▼
                                   ┌──────────┐
                                   │ Generate │
                                   │  signed  │
                                   │   URLs   │
                                   └────┬─────┘
                                        │
                                        ▼
                                   ┌──────────┐
                                   │  Return  │
                                   │   JSON   │
                                   └────┬─────┘
                                        │
                                        ▼
                                   ┌──────────┐
                                   │ Frontend │
                                   │ displays │
                                   │  reports │
                                   └──────────┘
```

---

## 🎯 Complete User Journey Example

### Patient Journey: From Signup to Getting Lab Report

```
Day 1: Registration
─────────────────────
User → http://localhost:5173/login
     → Enter email + password
     → Click "Sign Up"
     → Account created ✓

Admin → Supabase Dashboard
      → Find user
      → Set role = "patient"
      → Save ✓

Patient → Refresh page
        → Redirected to /dashboard/patient ✓

Day 2: Book Test (Placeholder)
──────────────────────────────
Patient → Dashboard → "Book Test"
        → Select test type
        → Schedule appointment
        → Request sent ✓

Day 3: Lab Uploads Report
─────────────────────────
Lab → Login as lab@dswf.org
          → /dashboard/lab
          → Click "Upload Report"
          → Select patient
          → Upload PDF file
          → Add remarks
          → Submit ✓

Backend → Validates file
        → Uploads to Supabase Storage
        → Saves to database
        → Creates notification ✓

Day 3: Patient Downloads Report
───────────────────────────────
Patient → Sees notification badge (1)
        → Clicks notification
        → "Your lab report is ready"
        → Clicks "Download"
        → PDF opens ✓

Patient → Can view report anytime
        → Secured with signed URL
        → Expires after 7 days
```

---

## 📊 Dashboard Access Matrix

```
Route                  | Admin | Patient | Donor | Lab | Student | Teacher | Pharmacy
─────────────────────────────────────────────────────────────────────────────────────
/                      |   ✓   |    ✓    |   ✓   |  ✓  |    ✓    |    ✓    |    ✓
/login                 |   ✓   |    ✓    |   ✓   |  ✓  |    ✓    |    ✓    |    ✓
/donation              |   ✓   |    ✓    |   ✓   |  ✓  |    ✓    |    ✓    |    ✓
/dashboard/admin       |   ✓   |    ✗    |   ✗   |  ✗  |    ✗    |    ✗    |    ✗
/dashboard/patient     |   ✓   |    ✓    |   ✗   |  ✗  |    ✗    |    ✗    |    ✗
/dashboard/donor       |   ✓   |    ✗    |   ✓   |  ✗  |    ✗    |    ✗    |    ✗
/dashboard/lab         |   ✓   |    ✗    |   ✗   |  ✓  |    ✗    |    ✗    |    ✗
/dashboard/student     |   ✓   |    ✗    |   ✗   |  ✗  |    ✓    |    ✗    |    ✗
/dashboard/teacher     |   ✓   |    ✗    |   ✗   |  ✗  |    ✗    |    ✓    |    ✗
/dashboard/pharmacy    |   ✓   |    ✗    |   ✗   |  ✗  |    ✗    |    ✗    |    ✓
```

---

**This visual guide shows how every part of your system works together!** 🎨

Use this to understand the complete flow from user action to database and back!

